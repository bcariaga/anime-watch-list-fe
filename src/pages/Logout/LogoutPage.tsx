import { Box, Heading } from '@chakra-ui/react';
import React from 'react';

const Logout = () => (
    <Box>
        <Heading noOfLines={1}>
            Logout
        </Heading>
    </Box>
);


export default Logout;